# Backend API base URL
VITE_API_URL=http://127.0.0.1:8000/api/v1

# Set to 'false' to use demo/local data for the Records feature (no backend needed)
# Omit or set to anything else to use the backend API
# VITE_RECORDS_API_ENABLED=true
